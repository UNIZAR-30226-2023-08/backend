///// LOGIN ///////

iniciar sesion = /token

json{
    username: 
    password:
}

DEVUELVE --> 
Token{  
        "access_token": access_token,  //GUARDARLO
        "token_type": "bearer"  
     }

Ese token a la hora de acceder a métodos con autentificacion tendreis que pasarlos en el header:

headers: {
    Authorization: `Bearer ${token}`,
}

//// EJEMPLO//////
import requests

url = 'https://ejemplo.com/api/endpoint'
token = 'TU_TOKEN_DE_AUTENTICACION'
parametro = 42

headers = {'Authorization': f'Bearer {token}'}
params = {'parametro': parametro}

response = requests.get(url, headers=headers, params=params)

if response.status_code == 200:
    print('La solicitud se realizó correctamente.')
else:
    print('Error:', response.status_code)

    


///// REGISTER ///////

registrarte = /register

json{
  "username": "string",
  "email": "string",
  "real_name": "string",
  "hashed_password": "string"
}